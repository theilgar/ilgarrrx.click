<!DOCTYPE html>
<html lang="az">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ilgarrrx serverinin sistem məlumatları</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto+Mono:wght@400;700&display=swap" rel="stylesheet">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/socket.io/4.0.1/socket.io.js"></script>
    <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">
</head>
<body>
    <div class="container">
        <div class="counters">
            <p>Ümumi Ziyarətçi Sayı: <span id="total_visits">{{ total_visits }}</span></p>
            <p>Online Ziyarətçilər: <span id="online_count">{{ online_count }}</span></p>
            <p>Mənim IP Adresim: 
                <span id="my_ip">
                    {% if user_ip_v4 and user_ip_v6 %}
                        {{ user_ip_v4 }} / {{ user_ip_v6 }}
                    {% elif user_ip_v4 %}
                        {{ user_ip_v4 }}
                    {% elif user_ip_v6 %}
                        {{ user_ip_v6 }}
                    {% else %}
                        Bilinmir
                    {% endif %}
                </span>
            </p>
        </div>

        <div class="section">
            <h2>Neofetch Məlumatları:</h2>
            <pre id="neofetch_output"></pre>
        </div>

        <button class="toggle-btn contact-btn" onclick="toggleVisitorInfo()">Ziyarətçi Məlumatlarını Göstər</button>
        <a href="https://www.instagram.com/ilgarrrx" target="_blank" class="contact-btn">Instagram</a>

        <div class="section visitor-info" id="visitor-section" style="display: none;">
            {% for ip_index, (ip, logs) in enumerate(visitor_logs.items(), start=1) %}
                <div class="visitor-log">
                    <span class="ip"><strong>{{ ip_index }}.</strong> {{ ip }} ({{ logs|length }} giriş, Son giriş: {{ logs[0].visit_time }})</span>
                    <button class="expand-btn contact-btn" onclick="toggleDetails('{{ ip }}')">+</button>
                </div>
                <div class="extra-info" id="details-{{ ip }}" style="display: none;">
                    {% for log in logs %}
                    <p>
                        <span class="device"><strong>Cihaz:</strong> {{ log.user_agent }}</span><br>
                        <span class="time"><strong>Ziyarət Vaxtı:</strong> {{ log.visit_time }}</span>
                    </p>
                    {% endfor %}
                </div>
                <hr>
            {% endfor %}
        </div>
    </div>

    <footer>
        <p id="author-text"></p>
    </footer>

    <script src="{{ url_for('static', filename='script.js') }}"></script>
</body>
</html>